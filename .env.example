# Copia para .env e preenche. NÃO versionar o .env.

# --- Supabase (obrigatório) ---
# Mesmo projeto do dashboard. Project Settings → API.
# Os lotes e formandos ficam no Postgres e os PDFs no bucket `contratos`: no
# Render o disco é efémero, e sem isto os links de assinatura já enviados
# morriam no primeiro restart. Corre o supabase_schema.sql uma vez.
# A service role key ignora RLS e só pode viver no servidor — nunca no
# browser, nunca commitada.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# URL pública da app (para os links de assinatura saírem corretos em produção).
# Em local podes deixar em branco.
BASE_URL=https://contratos.sportrail.pt

# Password da zona de admin (criar lotes / dashboard). Define AMBAS em produção
# senão a área de admin fica ABERTA a quem tiver o link. Em local podes deixar
# em branco (admin sem password). As páginas /assinar/<token> não usam isto.
ADMIN_USER=admin
ADMIN_PASS=

# Segredo partilhado que protege o endpoint POST /api/gerar-contrato (o motor
# de PDF chamado pelo dashboard). Tem de ser IGUAL ao CONTRATOS_PDF_TOKEN
# definido no dashboard. Sem isto, o endpoint responde 503.
PDF_API_TOKEN=

# ID da pasta-raiz DTP no Google Drive (do URL da pasta).
# Só necessário para arquivo real no Drive; sem isto o PDF fica no Supabase
# Storage, que já é arquivo durável.
DRIVE_ROOT_FOLDER_ID=
